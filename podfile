# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TextureExampleProject' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod "Texture"
  # Pods for TextureExampleProject

  target 'TextureExampleProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TextureExampleProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
